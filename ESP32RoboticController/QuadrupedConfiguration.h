#include "QuadrupedConfiguration.h"

// Constructor implementation
QuadrupedConfiguration::QuadrupedConfiguration()
    : legLength(0.0f), bodyWidth(0.0f), bodyLength(0.0f) {
    // Initialize your configuration with default values here
    // For example:
    legLength = 10.0f; // Default leg length
    bodyWidth = 5.0f;  // Default body width
    bodyLength = 12.0f; // Default body length
}

// You can add additional member function implementations here if needed.
